import { defineConfig, devices } from "@playwright/test";

// These are the flows that don't need a signed-in user — full authenticated
// flows (create org -> upload -> analyze) would need Clerk's testing-token
// setup (@clerk/testing) plus seeded test credentials, which needs its own
// Clerk test instance. See e2e/README.md for how to extend this.
export default defineConfig({
  testDir: "./e2e",
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: process.env.CI ? "github" : "list",
  use: {
    baseURL: "http://localhost:3000",
    trace: "on-first-retry",
  },
  projects: [
    {
      name: "chromium",
      use: { ...devices["Desktop Chrome"] },
    },
  ],
  webServer: {
    command: "npm run build && npm run start",
    url: "http://localhost:3000",
    reuseExistingServer: !process.env.CI,
    timeout: 180_000,
  },
});
